# Step: unless-web-run

Add and pass the web baseline for the generalized unless/do minted syntax demo.

## Work
1. Add reg-rs case d1_poc_unless_web_run for the web path running the unless/do fixture without code changes specific to that template.
2. Update the web demo/runtime path as needed so the baseline is GREEN.
3. Preserve all existing baselines.
4. Update docs/status.md.
5. Run sw-checklist before committing.

## Commit / complete protocol
Commit deliverables plus .agentrail/ with message:
web: run unless syntax
Then push and agentrail complete with reward 1.