# Step: interp-power-2-8-red

Create the M4 RED baseline for interpreting Power with
`n=2, e=8`, expecting `256`.

## Work
1. Add reg-rs case `d1_interp_power_2_8_eq_256`.
2. Capture the current RED output from the CLI scaffold.
3. Do not implement interpreter behavior in this step.
4. Update `docs/status.md`.
5. Run `sw-checklist` before committing.

## Commit / complete protocol
Commit deliverables plus `.agentrail/` with message:
`test: add power interpreter baseline`
Then push and `agentrail complete` with reward 1.
