# Step: library-grid-red

Create the M8 RED baseline for the library/dictionary grid.

Scope:
- Inspect the current Web snapshot helper pattern.
- Add the smallest reg-rs snapshot case for a future library grid HTML view of bundled definitions.
- The baseline should expect stable default ordering and visible columns for name, arity, type/category, and aspect/facet summary.
- Do not implement the grid in this step; leave the new case failing against current code.

Verification:
- Run only the targeted failing command and record the failure.

Commit message: test: add library grid red baseline